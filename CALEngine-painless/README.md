# CALEngine

A framework to build Continuous Active Learning processes.

# Installation

```bash
$ mkdir build && cd build && cmake .. && cmake --build .

# Run the tests
build/ $ ctest --verbose

# Binaries
build/ $ ls bin/
```
